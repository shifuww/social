<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Handlebars engine</title>
</head>
<body>
  <h1>{{title}}</h1>
  <h3>Num of users: {{lengthOfUsers}}</h3>
  <ul>
    {{#each users as |user|}}
    <li>
      <a href="/{{user.username}}">{{user.name}}</a>
    </li>
    {{/each}}
  </ul>
</body>
</html>